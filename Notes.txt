26/03/26
- Planned my architecture for my basic node set up 
- The architecture involves requests flowing towards a controller on the ASP.NET node I've set up, this controller will then try to place that request on the Ingress queue, this queue has a set max length to avoid the node being overwhelmed with requests, if there's not room on the queue the controller responds with a 429 error code or 202 status code if the task was added to the queue, I used a channel as my queue because it works very well for the publisher consumer pattern I'm setting up
- Next I created a work item dispatcher which will monitor this queue and then based on the execution type (CPU or IO bound) will pass that work item to the respective CPU bound or IO bound worker queue, these queues have different size constraints due to the nature of the work they perform. 
- If the dispatcher attempts to pass this work item to a queue that is full, the request will be dropped and logged, this isn't a permanent solution but for simplicity that what I've gone for for the time being.

30/03/26
- And now my architecture has completely shifted again
- Now i'm using the outbox pattern with a postgres database to store items, and a relayer service will push those tasks in batches to my RabbitMQ system that will then publish those messages to the worker nodes 

1/04/26
- If I'm to implement a lease based approach for grabbing tasks, I'll need to implement a fencing token so that any nodes that grab a task, pause, lose the lease and then try to complete the task are met with an error

2/04/26
- Need to fix cannot load library libgssapi_krb5 error, working fine but still annoying

3/04/26
- Finally got the outbox flow of my application working, a controller can upload a task to the database, the relay worker is then notified of that insert operation. Now the next step is for the relay worker to publish that event to RabbitMQ and then update the status of the task to published, this all needs to occur atomically so that if power was lost between these steps the system wouldn't be left in a weird state.
- Also need to figure out how I'm handling back pressure here, is it the controllers job to query the table for the number of published but not complete work items, we actually probably just want to test the number of unpublished work items, since the rabbit mq channel should be durable so these messages shouldn't be lost if the published is not null
- Need a way of managing different max number of unpublished counts across different controllers, should the number of allowed pending tasks be task type dependent?
- Need to implement a singleton service for creating and holding the relay rabbit mq connection, since it seems that it's expensive to create and destroy one constantly.
- Seems like the goal is to create a connection per application lifetime and then use a channel pool

4/04/26
- Need to batch work items to improve throughput but for the time being just sending one item across the channel at a time.
- Need to look at how I'm setting up my queue, what should i have a x-queue-type


6/04/26
- Got messages working, need to fix persistence in my rabbit MQ queue, currently when i shut down the worker messages build up in the queue, this survives a restart which works as expected however when I restart the consumer and process a few messages all messages move from Ready to unacked, if I restart here the messages are lost.

7/04/26
- Added outbox view instead of seperate table (this allows for values that still have a null published to be easily grabbed from the outbox view)
- Added publishedAt value being updated when ack is sent by the broker
- Fixed persistence issue now messages survive a restart once consuming starts

8/04/26
- Works quite well, I change the logic from a notification from postgres triggering a message send to having a 3 proned solution to batching and sending multiple messages, only one outbox clear process can occur at once but I currently have 3 things triggering a outbox process, a notification from postgres that a new item has been added, a timer and on start. All of these will trigger a run through of the entire outbox batching up items and sending them until there are no more in the outbox.
- The RabbitMQ message acking is working really well, currently I have it set up so that a we get the next message sequence number then loop through the batch adding that sequence number + the index of the item in the batch to a dictionary, then when an ack is received we use the delivery tag to remove the item from the dictionary. Currently multiple is always false i don't know if there is a way to trigger multiple = true when getting an ack from the broker. Currently though this works quite well
-Added a stale task view so that I can efficiently manage tasks that have been sent but not acked.

10/04/26
- Adding the stale task view and updating the schema to include a sentAt and a ackedAt so that values with no sent at are still sitting in the outbox while those will a stale sent at and no acked at are retried later on. This has made the system a lot more robust.
- Simulated the retry logic by ignoring an ack on every 3rd request, after a while all requests were successfully (some multiple times) and sent across. I need to implement idempotency on the client side now but the retry logic with exponential back off is working quite well.
- Need to make task Id something more replicatable, so if a user was to send the same request twice it would result in the same id rather than creating a new GUID everytime, for now though its suitable