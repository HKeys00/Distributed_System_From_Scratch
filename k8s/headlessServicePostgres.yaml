apiVersion: v1
kind: Service
metadata:
  name: postgresql-svc
spec:
  clusterIP: None
  selector:
    app: postgresql-a
  ports:
    - protocol: TCP
      port: 80
      targetPort: postgresql-port
    - protocol: TCP
      port: 5672
      targetPort: rabbitmq-port